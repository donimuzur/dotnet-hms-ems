@model IEnumerable<Sampoerna.EMS.Website.Models.WorkflowHistory.WorkflowHistoryViewModel>

<table id="workflowHistoryTable1" class="display table table-bordered table-striped" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th class="toggle-row">#</th>
            <th>Account Name</th>
            <th>Role</th>
            <th>Action</th>
            <th>Date</th>
            <th>Comment</th>
        </tr>
    </thead>
    @*<thead class="filters">
        <tr>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>*@
    <tbody>
        @{ int i = 1;}
        @foreach (var item in Model)
            {
            <tr>
                <td>@i</td>
                <td>@Html.DisplayFor(model => item.USERNAME)</td>
                <td>@Html.DisplayFor(model => item.Role)
                <td>
                    @Html.DisplayFor(model => item.ACTION)
                </td>
                <td>
                    @*@Html.DisplayFor(model => item.ACTION_DATE)*@
                    @if (item.ACTION_DATE != null)
                    {
                        @item.ACTION_DATE.Value.ToString("dd MMMM yyyy HH:mm:ss")
                    }
                </td>
                <td class="last">@Html.DisplayFor(model => item.COMMENT)</td>
            </tr>
            i++;
        }
    </tbody>
</table>
