@model Sampoerna.EMS.Website.Models.PRODUCTION.ProductionViewModel

<table id="DailyProduction" class="table table-striped table-hover" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th class="toggle-row"> Action </th>
            <th> Date </th>
            <th> Company </th>
            <th> Plant</th>
            <th> Finish Goods </th>
            <th> Description </th>
            <th> QtyPacked </th>
            <th> QtyUnpacked </th>
            <th> Uom </th>
        </tr>

    </thead>
    <thead class="filters">
        <tr>
            <th> Filter </th>
            <th> </th>
            <th> </th>
            <th> </th>
            <th> </th>
            <th> </th>
            <th> </th>
            <th></th>
            <th></th>

        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.Details)
        {
            <tr>
                <td class="action">
                    <a href="#" data-tooggle="tooltip" data-placement="top" title="Edit"><i class=" fa fa-pencil-square-o"></i></a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductionDate)
                </td>
                <td>
                  @Html.DisplayFor(modelItem => item.CompanyName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PlantName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FaCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BrandDescription)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.QtyPacked)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.QtyUnpacked)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Uom)
                </td>

            </tr>
        }
    </tbody>
</table>
