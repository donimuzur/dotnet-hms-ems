@model Sampoerna.EMS.Website.Models.LACK2.Lack2IndexPlantViewModel

<table id="lack1Table" class="display" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th class="toggle-row"> Filter </th>
            <th>Company</th>
            <th>Plant</th>
            <th>Tobacco Goods Type</th>
            <th>Supplier</th>
            <th>Period</th>
            <th>Status</th>
        </tr>
    </thead>
    <thead class="filters">
        <tr>
            <th>Action</th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Details)
        {
            <tr>
                <td class="action">
                    <a href="#"><i class="fa fa-eye"></i></a>
                    <a href="@Url.Action("Edit", "LACK2", new { id = item.Lack2Id})"><i class="fa fa-pencil-square-o"></i></a>
                </td>
                <td>
                    @Html.DisplayFor(model => item.Company)
                </td>
                <td>
                    @Html.DisplayFor(model => item.PlantId)
                </td>
                <td>
                    @Html.DisplayFor(model => item.TobaccoGoodType)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Supplier)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Period)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Status)
                </td>
            </tr>
        }
    </tbody>
</table>

<!--modal-->
@*<input id="btn-mastermaterial-modal" style="display: none" class="btn btn-blue" data-toggle="modal" data-target=".bs-example-modal-sm" value="Save">

    <div class="modal ems-modal fade bs-example-modal-sm" tabindex="-1" role="dialog" aria-labelledby="mySmallModalLabel">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
                    <h4 class="modal-title">Success</h4>
                </div>
                <div class="modal-body" id="modal-mastermaterial-message">

                </div>
            </div>
        </div>
    </div>*@